{
  "name": "circuit-breaker-wrapper",
  "version": "1.0.0",
  "description": "Wrap an async function call for use with a circuit breaker",
  "main": "index.js",
  "scripts": {
    "test": "eslint . && istanbul cover --report lcov _mocha -- --ui bdd --check-leaks --recursive --slow 50 --reporter spec test",
    "lint": "eslint .",
    "coveralls": "cat ./coverage/lcov.info | coveralls"
  },
  "author": "Springworks",
  "repository": {
    "type": "git",
    "url": "git://github.com/Springworks/node-circuit-breaker-wrapper.git"
  },
  "bugs": {
    "url": "https://github.com/Springworks/node-circuit-breaker-wrapper/issues"
  },
  "dependencies": {
    "circuit-breaker-js": "^0.0.1"
  },
  "devDependencies": {
    "chai": "^4.1.0",
    "coveralls": "^2.11.1",
    "eslint": "^4.3.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.5.0",
    "sinon": "^2.4.1",
    "sinon-chai": "^2.5.0"
  }
}
